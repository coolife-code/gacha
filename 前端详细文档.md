# 网页扭蛋机前端详细文档

## 1. 前端架构设计

### 1.1 技术选型
- 微信小程序原生开发（WXML + WXSS + JavaScript）
- WeUI组件库
- 小程序原生状态管理
- 微信小程序动画API

### 1.2 项目目录结构
```
src/
├── components/           # 公共组件
│   ├── gacha-machine/    # 扭蛋机组件
│   ├── url-card/         # 网址卡片组件
│   ├── user-card/        # 用户卡片组件
│   └── navigation/       # 导航组件
├── pages/                # 页面文件
│   ├── gacha/            # 扭蛋机页面
│   ├── profile/          # 个人中心
│   ├── social/           # 社交页面
│   └── detail/           # 详情页面
├── utils/                # 工具函数
├── api/                  # 接口封装
├── styles/               # 全局样式
├── assets/               # 静态资源
└── app.js               # 应用入口文件
```

## 2. 核心页面设计

### 2.1 扭蛋机页面（gacha）
- **功能**：每日免费抽取网址（限5次），展示抽取结果
- **主要元素**：扭蛋机模型、抽取按钮、剩余次数显示、抽取记录、规则说明
- **交互**：点击抽取按钮触发动画，展示结果，支持收藏/分享

### 2.2 社交页面（social）
- **功能**：展示微信好友列表和关注的人的动态，支持社交互动
- **主要元素**：
  - 微信好友列表：展示当前小程序用户的微信好友
  - 关注的人的动态流：展示已关注用户的收藏分享、评论等动态
  - 动态过滤器：可切换查看所有动态、仅微信好友动态或仅关注用户动态
- **交互**：
  - 点击微信好友进入其个人页面
  - 点击关注的人动态卡片中的网址卡片进入详情页面
  - 点击动态卡片中的用户头像进入该用户的个人页面
  - 点击评论按钮参与互动

### 2.3 用户个人页面（profile）
#### 可复用设计模式
- **功能**：展示用户信息、收藏列表和社交关系（粉丝和关注）
- **复用机制**：通过传入userId参数区分是查看自己还是其他用户的个人页面

#### 内容与交互
- **通用元素**：用户头像、昵称、个人简介、关注数/粉丝数（可点击查看列表）、统计数据（抽取次数、收藏数量、发布动态数）
- **核心内容**：收藏的网页卡片列表（页面主要部分），由多个UrlCard组件组成
- **社交列表**：
  - 关注列表：展示用户关注的所有人，由多个UserCard组件组成
  - 粉丝列表：展示关注该用户的所有人，由多个UserCard组件组成
- **当前用户特有**：设置选项、头像更换、资料编辑
- **其他用户特有**：关注/已关注按钮、私信按钮
- **交互**：
  - 点击收藏列表中的网址卡片进入详情页面
  - 点击关注数查看关注列表
  - 点击粉丝数查看粉丝列表
  - 点击关注/粉丝列表中的用户头像或卡片进入该用户的个人页面
  - 点击统计数据查看详情
  - 点击关注/取消关注
  - 点击头像进入主页

#### 性能优化
- **数据缓存**：缓存用户基本信息和统计数据
- **分页加载**：收藏列表分页加载
- **懒加载**：图片和非首屏内容懒加载

### 2.4 详情页面（detail）
- **功能**：展示网址详细信息、相关推荐和评论功能
- **主要元素**：网址信息卡片、相关推荐列表、评论列表、评论输入框
- **交互**：点击跳转、收藏分享、发表评论、评论回复
- **性能优化**：评论列表分页加载、图片懒加载、数据预加载
- **用户体验**：加载状态反馈、空状态处理、错误重试机制

## 3. 核心组件设计

### 3.1 扭蛋机组件（GachaMachine）
- **功能**：实现扭蛋机视觉和交互效果
- **关键属性**：disabled、dailyDrawCount、maxDailyDrawCount、nextResetTime
- **主要方法**：startDraw、showResult、resetDailyCount
- **核心事件**：drawStart、drawEnd、countReset

### 3.2 网址卡片组件（UrlCard）
#### 架构设计
- **功能**：展示网址信息，支持跳转和交互
- **组件化思想**：采用独立状态管理，支持多场景复用
- **扩展性**：预留自定义样式和行为扩展点
- **可测试性**：组件逻辑与视图分离，便于单元测试

#### 核心API
- **关键属性**：title、url、description、tags、isCollected、favicon、domain、showActions、cardStyle
- **主要方法**：collect()、share()、openUrl()、copyUrl()、recordView(duration)
- **核心事件**：collectChange、shareClick、urlClick、viewRecorded

### 3.3 评论组件（CommentList）
#### 架构设计
- **功能**：展示评论列表、支持评论发布和回复
- **数据流**：采用单向数据流设计，状态集中管理
- **扩展性**：支持自定义评论模板和操作按钮

#### 核心API
- **关键属性**：urlId、comments、pageSize、showPublish、sortBy、maxLength
- **主要方法**：publishComment(content, parentId)、loadMoreComments()、likeComment(commentId)、deleteComment(commentId)、replyToComment(comment)
- **核心事件**：commentAdded、commentLiked、commentDeleted、loadMore
#### 视觉与交互设计
1. **卡片布局**
   - 顶部：网站图标 + 标题（限制1行，超出省略）
   - 中部：描述信息（限制2行，超出省略）
   - 底部：标签展示区 + 操作按钮

2. **交互设计**
   - 点击卡片主体区域直接跳转到网址
   - 长按卡片显示操作菜单（收藏、分享、复制链接）
   - 点击收藏按钮切换收藏状态（带动画效果）
   - 点击分享按钮调用系统分享功能
   - 网址打开时开始计时，关闭时记录浏览时长

3. **无障碍支持**
   - 支持屏幕阅读器
   - 提供键盘操作支持
   - 高对比度模式适配

#### 性能与缓存策略
- **数据缓存**：
  - 首次访问时从服务器获取完整信息并缓存
  - 后续访问优先从本地缓存读取（有效期72小时）
  - 缓存过期后重新请求服务器获取最新数据
  - 用户收藏操作实时更新本地缓存状态

- **渲染优化**：
  - 图片懒加载（特别是favicon）
  - 长列表中使用虚拟列表技术
  - 避免不必要的重渲染
  - 事件委托减少事件监听器数量

#### 错误处理机制
- 图片加载失败时显示默认图标
- 网络请求失败时提供重试选项
- URL格式验证和错误提示
- 分享功能不可用时的降级处理

### 3.3 评论组件（CommentList）扩展
#### 交互设计
- 点击评论可展开回复
- 支持长按评论显示操作菜单
- 评论输入框自动适配高度
- 滑动到底部自动加载更多


#### 性能与安全
- 评论列表分页加载和虚拟滚动
- 输入内容长度和格式验证
- 防XSS攻击处理
- 重复提交限制
- 敏感词过滤

### 3.4 用户个人资料组件（UserProfile）
#### 架构设计
- **功能**：展示用户详细信息、收藏列表和社交关系（粉丝和关注），支持社交互动
- **组件化思想**：采用独立状态管理，支持多场景复用
- **条件渲染**：根据是否为当前用户显示不同内容和功能
- **组件嵌套**：集成UrlCard组件展示收藏的网页卡片，集成UserCard组件展示用户列表
- **列表切换**：支持在收藏列表、关注列表和粉丝列表之间切换展示

#### 核心API
- **关键属性**：userId、isCurrentUser、userInfo、statsData、collections、followingList、followersList、activeTab
- **主要方法**：followUser()、unfollowUser()、editProfile()、loadMoreCollections()、loadMoreFollowing()、loadMoreFollowers()、switchTab(tabName)
- **核心事件**：followChange、profileUpdated、collectionClicked、userClicked、tabSwitched

### 3.5 用户卡片组件（UserCard）
#### 功能描述
用于展示用户信息的卡片组件。

#### 属性
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| avatar | String | '' | 用户头像 |
| nickname | String | '' | 用户昵称 |
| isFollowing | Boolean | false | 是否已关注 |
| stats | Object | {} | 统计信息 |

#### 方法
| 方法名 | 参数 | 描述 |
|--------|------|------|
| follow | - | 关注用户 |
| sendMessage | - | 发送消息 |

#### 事件
| 事件名 | 参数 | 描述 |
|--------|------|------|
| followChange | isFollowing | 关注状态改变事件 |

## 4. 状态管理设计

### 4.1 全局状态
```
store/
├── user.js          # 用户状态
├── gacha.js         # 扭蛋机状态
├── collection.js    # 收藏状态
├── social.js        # 社交状态
│   ├── friends.js   # 微信好友状态
│   ├── follows.js   # 关注关系状态
│   └── feeds.js     # 动态流状态
└── preferences.js   # 用户偏好状态
```

### 4.2 用户状态（user.js）
```javascript
const userState = {
  # 模块化设计方案



   
### 4.3 模块化优势

### 4.6 本地存储策略
为了提升用户体验和减少不必要的网络请求，前端采用以下本地存储策略：

#### 4.6.1 用户信息缓存
- **缓存内容**：用户基本信息（昵称、头像等）、登录状态、token信息
- **缓存时长**：24小时
- **更新机制**：用户登录成功后立即更新，超过缓存时长后重新请求
- **存储位置**：localStorage
- **首次登录特殊处理**：首次登录成功后立即缓存完整用户数据，确保后续访问无需重新请求

#### 4.6.2 抽取状态缓存
- **缓存内容**：每日抽取次数、下次重置时间
- **缓存时长**：至当日24:00
- **更新机制**：每次抽取后更新本地状态，每日首次打开应用时检查是否需要重置
- **存储位置**：localStorage

#### 4.6.3 收藏列表缓存
- **缓存内容**：用户收藏的网址列表
- **缓存时长**：1小时
- **更新机制**：用户进行收藏/取消收藏操作后同步更新本地缓存
- **存储位置**：localStorage

#### 4.5.4 网址详情缓存
- **缓存内容**：已查看过的网址详情信息
- **缓存时长**：永久
- **更新机制**：访问网址详情时优先从缓存读取
- **存储位置**：localStorage

#### 4.6.5 社交动态缓存
- **缓存内容**：微信好友列表、关注的人动态列表
- **缓存时长**：30分钟
- **更新机制**：下拉刷新或超过缓存时长后重新请求
- **存储位置**：localStorage

## 5. API接口设计

### 5.1 核心接口

## 6. 动画与交互

### 6.1 扭蛋机动画流程
- 按钮按下 → 扭蛋机旋转 → 减速停顿 → 扭蛋弹出 → 卡片展开
- 全程伴随相应音效增强体验

### 6.2 页面过渡效果
- 主页面：左右滑动切换
- 模态框：淡入淡出
- 详情展开：缩放效果

## 7. 性能优化要点

### 7.1 渲染优化
- 延迟加载图片和非首屏内容
- 长列表使用虚拟化渲染
- 避免不必要的DOM操作和样式修改
- 合理复用组件结构

### 7.2 网络优化
- 本地缓存不经常变化的数据
- 列表数据分页获取
- 合并减少不必要的网络请求
- 图片资源压缩优化

### 7.3 安全措施
- 接口鉴权确保身份验证
- 严格验证用户输入
- 限制操作频率防止恶意行为

## 8. 兼容性与适配方案

### 8.1 设备适配
1. **屏幕适配**
   - 使用rpx单位适配不同屏幕
   - 响应式布局设计

2. **系统适配**
   - iOS/Android兼容性处理
   - 不同版本微信兼容性

### 8.2 版本兼容
1. **API兼容**
   - 新API降级处理
   - 功能开关控制

2. **样式兼容**
   - flex布局兼容性处理
   - 样式前缀处理

## 9. 测试方案

### 9.1 单元测试
1. **工具函数测试**
   - 数据处理函数测试
   - 格式化函数测试

2. **组件测试**
   - 组件属性测试
   - 组件方法测试

### 9.2 UI测试
1. **页面布局测试**
   - 不同设备尺寸测试
   - 文字长度适配测试

2. **交互流程测试**
   - 核心功能流程测试
   - 异常流程测试

### 9.3 性能测试
1. **首屏加载时间**
   - 各页面加载时间监控
   - 关键路径性能优化

2. **内存占用测试**
   - 页面切换内存泄漏检测
   - 图片缓存优化测试

## 10. 部署与发布

### 10.1 构建流程
1. **代码检查**
   - ESLint代码规范检查
   - 样式规范检查

2. **打包优化**
   - 代码压缩
   - 资源压缩

### 10.2 发布流程
1. **版本管理**
   - Git分支管理策略
   - 版本号规范

2. **灰度发布**
   - 小范围用户测试
   - 逐步扩大发布范围

---

*文档版本：v1.0*
*最后更新日期：2025年10月21日*